h3(v-if='selectedTags.length != 0') Filtered by tag: {{selectedTag}}
h3(v-if='search.length != 0') Search results for {{search}}
h1 Saved Sessions - There are {{tabsNumber}} saved tabs.
h3(v-if='state.groups.length == 0') There are no saved sessions!
button(@click='createNewGroup') Add Group